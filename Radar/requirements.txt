# numpy capped <2 and opencv <4.12 so they stay compatible with nuscenes-devkit
# (requires numpy<2). opencv >=4.12 / numpy >=2 would conflict.
numpy>=1.22,<2
torch>=1.10
scikit-learn>=1.0
opencv-python-headless>=4.5,<4.12
nuscenes-devkit>=1.1
pyquaternion>=0.9
streamlit>=1.20
